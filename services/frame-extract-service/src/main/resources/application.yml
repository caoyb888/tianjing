server:
  port: 8098

spring:
  application:
    name: frame-extract-service
  kafka:
    bootstrap-servers: ${TIANJING_KAFKA_BOOTSTRAP_SERVERS:kafka:9092}
    consumer:
      auto-offset-reset: latest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer

management:
  endpoints:
    web:
      exposure:
        include: health,prometheus

tianjing:
  jwt:
    public-key-pem: ${TIANJING_JWT_PUBLIC_KEY_PEM:}
  frame-extract:
    # 默认帧采样间隔（毫秒）：200ms ≈ 5fps，可通过 Nacos 场景配置覆盖
    default-interval-ms: ${TIANJING_FRAME_EXTRACT_INTERVAL_MS:200}
