# 集成测试专用 Profile（-Pintegration-test 激活）
# 使用 Testcontainers PostgreSQL 替代真实数据库，排除 Redis 防止连接失败
spring:
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration
      - org.springframework.boot.autoconfigure.data.redis.RedisRepositoriesAutoConfiguration
  # 占位值，会被 @DynamicPropertySource 覆盖为 Testcontainer 地址
  datasource:
    url: jdbc:postgresql://localhost:5432/tianjing_test
    username: test
    password: test
    driver-class-name: org.postgresql.Driver
  flyway:
    enabled: true
    locations: filesystem:../../deploy/flyway/prod
    baseline-on-migrate: false
    out-of-order: false

tianjing:
  jwt:
    public-key-pem: ""   # 空串：JwtTokenProvider 跳过 RSA 密钥初始化

logging:
  level:
    com.tianzhu.tianjing: WARN
    org.flywaydb: INFO
